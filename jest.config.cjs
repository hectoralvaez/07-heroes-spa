module.exports = {
    testEnvironment: 'jest-environment-jsdom',
    // setupFiles: ['./jest.setup.js'] // No usamos fetch API, no necesitamos ninguna configuración, s como unas variables de entorno
}
